/// 15

comptime proc sum_to(n int) int {
  var total int = 0;
  var i int = 1;
  while i <= n {
    total = total + i;
    i = i + 1;
  }
  return total;
}

proc main() int {
  return comptime sum_to(5);
}
